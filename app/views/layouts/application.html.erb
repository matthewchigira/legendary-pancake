<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SlickCraft" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      :root {
        --primary: #6366F1;
        --primary-hover: #4F46E5;
        --bg: #F8FAFC;
        --surface: #FFFFFF;
        --slate-900: #0F172A;
        --slate-700: #334155;
        --slate-500: #64748B;
        --slate-300: #CBD5E1;
        --slate-200: #E2E8F0;
        --slate-100: #F1F5F9;
        --text: #0F172A;
        --border: #E2E8F0;
        --success: #10B981;
        --success-bg: #ECFDF5;
        --danger: #EF4444;
        --danger-bg: #FEF2F2;
        --warning-bg: #FFFBEB;
        --warning-border: #FCD34D;
      }
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        background-color: var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        -webkit-font-smoothing: antialiased;
      }
      header {
        background: var(--surface);
        color: var(--slate-900);
        padding: 1rem 2rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        border-bottom: 1px solid var(--border);
      }
      header h1 { font-size: 1.35rem; font-weight: 700; letter-spacing: -0.025em; }
      header h1 a { color: var(--slate-900); text-decoration: none; }
      header h1 a span { color: var(--primary); }
      header nav a {
        color: var(--slate-500);
        text-decoration: none;
        margin-left: 1.5rem;
        font-weight: 500;
        font-size: 0.9rem;
        transition: color 0.15s;
      }
      header nav a:hover { color: var(--primary); }
      main {
        flex: 1;
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        width: 100%;
      }
      .flash {
        padding: 0.75rem 1rem;
        border-radius: 8px;
        margin-bottom: 1.5rem;
        font-weight: 500;
        font-size: 0.9rem;
      }
      .flash-notice {
        background: var(--success-bg);
        border: 1px solid var(--success);
        color: #065F46;
      }
      .flash-alert {
        background: var(--danger-bg);
        border: 1px solid var(--danger);
        color: #991B1B;
      }
      footer {
        background: var(--slate-900);
        color: var(--slate-500);
        text-align: center;
        padding: 1.25rem;
        font-size: 0.8rem;
        letter-spacing: 0.025em;
      }
      a { color: var(--primary); }
      .btn {
        display: inline-block;
        padding: 0.5rem 1.25rem;
        border: none;
        border-radius: 8px;
        font-size: 0.875rem;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        transition: all 0.15s ease;
        letter-spacing: -0.01em;
      }
      .btn:hover { transform: translateY(-1px); box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
      .btn-primary { background: var(--primary); color: white; }
      .btn-primary:hover { background: var(--primary-hover); }
      .btn-secondary { background: var(--slate-100); color: var(--slate-700); }
      .btn-secondary:hover { background: var(--slate-200); }
      .btn-danger { background: var(--danger); color: white; }
      .btn-sm { padding: 0.3rem 0.75rem; font-size: 0.8rem; }
    </style>
  </head>

  <body>
    <header>
      <h1><a href="<%= root_path %>"><span>Slick</span>Craft</a></h1>
      <nav>
        <a href="<%= recipes_path %>">Recipes</a>
        <a href="<%= new_recipe_path %>">+ New</a>
      </nav>
    </header>

    <main>
      <% flash.each do |type, message| %>
        <div class="flash flash-<%= type %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </main>

    <footer>
      SlickCraft &mdash; Modern Recipe Collection
    </footer>
  </body>
</html>
